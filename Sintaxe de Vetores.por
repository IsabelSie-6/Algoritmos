programa {
  funcao inicio() { 
    // Sintaxe: tipo nome[tamanho]

    inteiro notas[5]
    real alturas [10]
    cadeia nomes[3] 
    logico respostas[4] 

    // Atribuindo valores individualmente: 
    nomes[0] = " Ana"
    nomes[1] = " Bruno"
    nomes[2] = " Carla" 

    escreva(nomes[0]) // Ana
    escreva(nomes[2]) // Carla
    
  }
}
