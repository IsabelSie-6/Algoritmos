programa {
  funcao inicio() { 
    inteiro numeros[5]
    numeros[0] = 10    // primeira posição
    numeros[1] = 20    // segunda posição
    numeros[2] = 30    // terceira posição 
    numeros[3] = 40    // quarta posição
    numeros[4] = 50    // quinta posição

    escreva(numeros[3]) // imprime 30
  }
}
